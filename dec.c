#include<stdio.h>
int main(){// main function is the starting point of the execution of the c program
    int a=10;// Declares avariable and initialize the value
    int b=--a;// Decrement opreator is used.
    printf("a=%d\n",a);// first decreases the value.
    printf("b=%d\n",b);// then assigns to the b.
    return 0;//End of the program.
}